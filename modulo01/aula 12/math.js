/*
    Math -> Matemática

    - pow(2,2) / potência / 2² = 4
    - sqrt(25,2) / raiz / 25
    - ceil / teto -> arredondar pra cima
    - floor / chão -> arredonda pra baixo
*/

// pow
const potencia = Math.pow(2,5)
// console.log(potencia)

// sqrt
const raiz = Math.sqrt(125,2)
console.log(Math.ceil(raiz))
